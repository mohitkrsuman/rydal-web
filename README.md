# Rydal

Marketing site for Rydal — the riding assistant and riders' companion app for safe group riding, built with [Astro](https://astro.build).

## Run locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Start the dev server:
   `npm run dev`
3. Build for production:
   `npm run build`
